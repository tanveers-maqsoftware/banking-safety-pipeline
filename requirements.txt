# Safety pipeline
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
lingua-language-detector==2.2.0

# Presidio's NLP engine. The model is not on PyPI -- see README for the
# `spacy download en_core_web_lg` step, which must run after this file.
spacy==3.8.14

# Service layer
fastapi==0.140.8
uvicorn[standard]==0.51.0
pydantic==2.13.4

# Tests
pytest==9.1.1
httpx==0.28.1
